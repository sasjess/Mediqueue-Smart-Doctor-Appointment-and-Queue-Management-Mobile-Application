enum UserRole {
  patient,
  receptionist,
  admin,
}

enum BookingStatus {
  pending,
  confirmed,
  waiting,
  arrived,
  serving,
  completed,
  cancelled,
  late,
}

enum DutyStatus {
  scheduled,
  arrived,
  runningLate,
  onBreak,
  completed,
  cancelled,
}